// import { fromJS } from 'immutable';
// import { makeSelectLunaDeMielDomain } from '../selectors';

// const selector = makeSelectLunaDeMielDomain();

describe('makeSelectLunaDeMielDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
