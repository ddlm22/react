// import { fromJS } from 'immutable';
// import { makeSelectMaestrosDomain } from '../selectors';

// const selector = makeSelectMaestrosDomain();

describe('makeSelectMaestrosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
